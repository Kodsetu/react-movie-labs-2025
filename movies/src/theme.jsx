import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: "#111111",
    secondary: "#0f0f0f",
    tertiary: "#addae6",
    text: {
      primary: "#ffffff",
      secondary: "#000000",
      tertiary: "#addae6",
    },
  },
});

export default theme;